using UnityEngine;

[CreateAssetMenu(fileName = nameof(SOSources), menuName = "SO/" + nameof(SOSources), order = 0)]
public class SOSources : ScriptableObject
{
    public string Sources1;
    public string Sources2;
}
